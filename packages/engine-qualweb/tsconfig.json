{
  "extends": "@ally/config/tsconfig/browser.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    // The adapter resolves QualWeb's browser bundles from disk before injecting them.
    "types": ["node"]
  },
  "include": ["src/**/*.ts"],
  "exclude": ["dist", "node_modules"]
}
