{
  "name": "@ally/core",
  "version": "0.0.0",
  "type": "module",
  "description": "Framework-agnostic domain model and engine contracts for Ally",
  "license": "Apache-2.0",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@ally/config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
